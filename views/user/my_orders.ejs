<%- include('navbar.ejs') %>

<div class="hero">
    <div class="container">
        <div class="row justify-content-between">
            <div class="col-lg-5">
                <div class="intro-excerpt">
                    <h1>My Orders </h1>
                </div>
            </div>
            <div class="col-lg-7">

            </div>
        </div>
    </div>
</div>

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
    
        </div>
        <% for(let i=0; i<orders.length; i++) { %>
        <div class="col-md-4 mt-3">
            <a href="/print_order/<%=orders[i].order_id%>" style="text-decoration: none;">
            <div class="order_box shadow border p-3 mt-4" style="color: black;">
                <div style="text-align: center; text-transform: capitalize; background-color:#385D50; color: white;">
                    <%=orders[i].order_status%>
                </div>
                #FURNI <%=orders[i].order_id%>
                <span class="float-end">
                    <%=orders[i].order_date%>
                </span><br>
                &#8377; <%=orders[i].total_amt%>
                <span class="float-end" style="text-transform: capitalize;">
                    Payment : <%=orders[i].payment_status%>
                </span>
                
            </div>
            </a>
        </div>

        <%}%>
    </div>
</div>

<br><br><br><br><br>



<!-- SELECT *, (SELECT SUM(qty* price) FROM order_products 
    WHERE order_products.order_id = order_tbl.order_id) 
                AS total_amt FROM order_tbl 

inner Query :    SELECT SUM (qty*price)FROM order_products WHERE 
    order_products.order_id = order_tbl.order_id

-->


<%- include('footer.ejs') %>